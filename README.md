# TextRank-Based Sentence Extraction

Demonstrates sentence extraction using the TextRank algorithm.

## Setup

Install dependencies and download the required spaCy model:

```bash
pip install -r requirements.txt
python -m spacy download en_core_web_sm
```

## Usage

Run the demo script:

```bash
python textrank_sentence_extraction.py
```

